---
- name: install, start, and enable httpd
  hosts: all
  tasks:
  - name: install httpd package
    yum:
      name: httpd
      state: latest
  - name: start and enable httpd service
    service:
      name: httpd
      state: started
      enabled: yes
